HOST            : cannery.${tld}
PORT            : 4000
DATABASE_URL    : ecto://postgres:${postgres_password}@postgres.${consul_tld}:5432/cannery?sslmode=disable
POOL_SIZE       : 10
SECRET_KEY_BASE : ${cannery_secret_key_base}
REGISTRATION    : public
LOCALE          : en_US
SMTP_HOST       : smtp.gmail.com
SMTP_PORT       : 587
SMTP_USERNAME   : ${email}
SMTP_PASSWORD   : xa2yP$Xv%szL2zDbmZBC
SMTP_SSL        : true
  